"use client";

export type MetadataProps = { projectId: string };

export default function Metadata({ projectId }: MetadataProps) {
  return (
    <div className="p-4">
      {/* Metadata panel – empty for now */}
    </div>
  );
}
